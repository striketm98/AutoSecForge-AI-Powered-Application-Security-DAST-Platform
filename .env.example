# ── AutoSecForge Pro v12.1 – .env.example ────────────────────────
# Copy this to .env and fill in all CHANGE_ME_ values.
# Generate secrets: openssl rand -base64 32
# NEVER commit .env to Git.

APP_NAME=AutoSecForge
APP_ENV=production
APP_DEBUG=false
APP_URL=https://autosecforge.com

DB_HOST=db
DB_PORT=3306
DB_NAME=security_dashboard
DB_USER=dashboard
DB_PASSWORD=CHANGE_ME_DB_PASSWORD
DB_ROOT_PASSWORD=CHANGE_ME_DB_ROOT_PASSWORD

SESSION_SECRET=CHANGE_ME_SESSION_SECRET
APP_KEY=CHANGE_ME_APP_KEY

# ZAP API Key - MANDATORY (ASF-009). Used by the zap service + mcp-router.
ZAP_API_KEY=CHANGE_ME_ZAP_API_KEY

# MobSF REST API key - fixes a deterministic key for the mobile scanner page.
# Generate: openssl rand -hex 32   (same value used by the mobsf + app services)
MOBSF_API_KEY=CHANGE_ME_MOBSF_API_KEY

# ── Ollama (local LLM) ────────────────────────────────────────────
# After first docker compose up, pull a model that fits your RAM:
#   docker exec autosecforge-ollama ollama pull qwen2.5:1.5b
# llama3 (8B) needs ~5-6 GB; on a 3.8 GB box use qwen2.5:1.5b.
OLLAMA_MODEL=qwen2.5:1.5b

# ── Internal service URLs (Docker network) ────────────────────────
MCP_URL=http://mcp-router:6300
AI_AGENT_URL=http://ai-agent:6400
OASM_URL=http://oasm:6200
SQLMAP_URL=http://sqlmap:6000
